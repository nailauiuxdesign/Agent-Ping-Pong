{
  "name": "coral-test-app",
  "private": true,
  "version": "1.0.0",
  "description": "Test application for Coral Protocol and Feed Monitor Pipeline",
  "scripts": {
    "start": "vite --host 0.0.0.0 --port 3000",
    "dev": "vite --host 0.0.0.0 --port 3000",
    "preview": "vite preview --host 0.0.0.0 --port 3000"
  },
  "devDependencies": {
    "vite": "^5.4.1"
  }
}